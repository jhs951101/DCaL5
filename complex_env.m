function y=complex_env(x, ts, T1, T2, fc)% ---------------------------------------------------------% complex_env.m% This function returns the lowpass equivalent of the input bandpass signal%		x = input bandpass signal%		ts = sampling interval%		[T1,T2] = observation time interval%		fc = center frequency% ---------------------------------------------------------t = [T1:ts:T2];	z = hilbert(x);  % Get analytic signaly = z.*exp(-j*2*pi*fc*t);  % Complex envelope